<Window x:Class="DevApps.GUI.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="À propos" Height="220" Width="380"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="20" FontWeight="SemiBold" Text="DevApps"/>
        <TextBlock Grid.Row="1" Margin="0,16,0,0" Text="{Binding VersionText}"/>
        <TextBlock Grid.Row="2" Margin="0,8,0,0" Text="{Binding AuthorText}"/>
        <TextBlock Grid.Row="3" Margin="0,16,0,0" TextWrapping="Wrap" Foreground="DimGray">
            Outil de conception et d'édition pour les projets DevApps.
        </TextBlock>
        <Button Grid.Row="4" Width="90" Margin="0,20,0,0" HorizontalAlignment="Right" IsDefault="True" IsCancel="True" Click="CloseButton_Click">Fermer</Button>
    </Grid>
</Window>
