<UserControl x:Class="DevApps.GUI.DesignerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DevApps.GUI"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="{Binding CommandPanelHeight, Mode=OneWay}"></RowDefinition>
        </Grid.RowDefinitions>
        <Canvas Grid.Row="0" x:Name="MyCanvas" Background="White" MouseDown="Canvas_MouseDown" MouseMove="Canvas_MouseMove" MouseUp="Canvas_MouseUp" ClipToBounds="True" MouseWheel="Canvas_MouseWheel" AllowDrop="True" Drop="dataGrid_Drop">
        </Canvas>
        <Grid Grid.Row="1">
            <Border ToolTip="Liste des commandes" Cursor="SizeNS" HorizontalAlignment="Right" Background="Gray" Height="10" Width="60" CornerRadius="5,5,0,0" Margin="0,-10,0,0" MouseDown="Slider_MouseDown" MouseMove="Slider_MouseMove" MouseUp="Slider_MouseUp">
                <Canvas>
                    <Path Stroke="#DDDDDD">
                        <Path.Data>
                            <GeometryGroup>
                                <LineGeometry StartPoint="10,4" EndPoint="50,4"></LineGeometry>
                                <LineGeometry StartPoint="10,7" EndPoint="50,7"></LineGeometry>
                            </GeometryGroup>
                        </Path.Data>
                    </Path>
                    <Path Stroke="#707070">
                        <Path.Data>
                            <GeometryGroup>
                                <LineGeometry StartPoint="10,5" EndPoint="50,5"></LineGeometry>
                                <LineGeometry StartPoint="10,8" EndPoint="50,8"></LineGeometry>
                            </GeometryGroup>
                        </Path.Data>
                    </Path>
                </Canvas>
            </Border>
        </Grid>
        <Grid Grid.Row="2">
            <DataGrid ItemsSource="{Binding CommandsItems}" AutoGenerateColumns="False" CanUserAddRows="True" VerticalScrollBarVisibility="Visible">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}"></DataGridTextColumn>
                    <DataGridTemplateColumn Header="Commande" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Description}"></TextBlock>
                                    <TextBlock Margin="10,0,0,0" FontFamily="Verdana" FontSize="8pt" Foreground="Gray" Text="{Binding CommandLine}"></TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
