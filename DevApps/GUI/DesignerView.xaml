<UserControl x:Class="DevApps.GUI.DesignerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DevApps.GUI"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Canvas Grid.Row="0" x:Name="MyCanvas" Background="White" MouseDown="Canvas_MouseDown" MouseMove="Canvas_MouseMove" MouseUp="Canvas_MouseUp" ClipToBounds="True" MouseWheel="Canvas_MouseWheel" AllowDrop="True" Drop="dataGrid_Drop">
        </Canvas>
        <Grid Grid.Row="1">
            <Border HorizontalAlignment="Right" Background="Gray" Height="16" Width="60" CornerRadius="5,5,0,0" Margin="0,-16,0,0" MouseLeftButtonUp="Border_MouseLeftButtonUp">
                <TextBlock TextAlignment="Center" Foreground="White" Padding="0">⬍</TextBlock>
            </Border>
        </Grid>
        <Grid Grid.Row="2" MinHeight="100">
            <DataGrid ItemsSource="{Binding CommandsItems}" AutoGenerateColumns="False" CanUserAddRows="True" VerticalScrollBarVisibility="Visible">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}"></DataGridTextColumn>
                    <DataGridTemplateColumn Header="Commande" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Description}"></TextBlock>
                                    <TextBlock Margin="10,0,0,0" FontFamily="Verdana" FontSize="8pt" Foreground="Gray" Text="{Binding CommandLine}"></TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
